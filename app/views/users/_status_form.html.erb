<%= form_with(model: @user, local: true, url: update_status_user_path(@user)) do |f| %>
  <div class="field">
    <%= f.label :status %>
    <%= f.select :status, User::VALID_STATUS_LIST%>
  </div>

  <div class="actions">
    <%= f.submit "Change status" %>
  </div>
<% end %>

